Set:
set - It is a collection(UnOrdered. so, non indexed) of Objects.It is mutable. Doesn't allows duplicate.
Ex: {"Reshma",10,20}
1) Creating Set
2) Accessing values of set
3) Value exist in Set or not
4) Add items to set - add() - to add single item, update() - to add multiple item
5) Set length - len()
6) Remove item from Set - remove(), discard()
7) Clear the items in the list - clear(), del keyword
8) Join set - union(), update()
