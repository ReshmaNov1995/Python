11, 16 Is not required
Ignore 12a
Program 1 and 17 is same. Logic is different.
sort() - Used to sort list
sorted() - Used to sort the String

1) Find common letters b/w 2 strings
Remove duplicate
Use AND(&) operator to found common letters


2) Count the Frequency of words appearing in a String
split the string
declare empty dictionary
for loop i the split  # i is a key
 if i not in dict.keys()
  dict[i] = 0
 dict[i] = dict[i]+1


3) List to Dictionary
dict(zip(key,value))
Dictionary to Tuple
for i in dict.items():


4) Find 1 Missing number in an array
summation
n*(n+1)//2 - sum(array)
n = a[-1]
a = [1,2,3,5,6,7]


5) Find out pairs with given sum value of an array
arr.sort() 
while left<= right
left + right > sum
right-1
left + right < sum
left+1
left + right = sum
right-1
left+1


6) Find minimum difference b/w 2 elements of array
sort
for length
        if (arr[i+1] - arr[i] < min_diff):
            min_diff = arr[i+1] - arr[i]


7) Find maximum difference b/w 2 elements of array
same as above


8) PostFix Expression
stack = []
define operator
for arr
        if item not in operator: # Operand
            stack.append(item) 
        else: # Operator
            first = int(stack.pop())
            second = int(stack.pop())
            if (item == "+"):
                stack.append(second + first)


9) Length of Last word
split
len(arr[-1])


10) Remove duplicate from sorted array
length
list = []
for length
        if arr[i] != arr[i+1]:
            list_a.append(arr[i])
list_a.append(arr[n-1])


